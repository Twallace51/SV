# TODO for generic-mainmenu project

update for only essentials in requirements and vscode-extensions text files
decide if venv required or not

make sure all falsegrado fields have been set to default 0 
